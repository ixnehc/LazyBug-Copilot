0.11~0.13.2

Support multiple keywords in text search and symbol query
User message now shows the tag instead of the plain text
Fix the "thought signature" issue of Gemini api format
Fix a cost statistic mistake for OpenAI-compatible LLM api format
Add a "modified files" summarize frame at the end of each session

0.14
Image attachment support, you can now paste image or image file into the chat input window
Chat input window improvement/bug fix. (tag copy-paste, undo-redo)
Fix a symbol parsing issue resulting in function body not fully retrieved.

0.15
- C# symbol supported. LLM can now query C# symbols(function name,class name,...)
- Add new tool of SearchFilePath
- Display thumbnail for image tags
- Support symbol link recognization in chat window
- Add DeepSeek V4 support
	[Provider]
	name=DeepSeek
	endpoint=https://api.deepseek.com/chat/completions
	apiFormat=DeepSeek
- Chat input window tag-related bug fixes.
- Fix a gemini api tool-call naming issue
- Fix a code-database service stalling issue
- Fix the issue that thinking mode could never be enabled for anthropic models

0.16
- Add new tool of CLI (cmd.exe , bash.exe , python.exe)
- Add new tool of AskQuestion
- Add skill support and a panel to manage them
- Fix the cost statistic getting reset issue
- Remove dependency on mfc dll
- Support 中文字符的 file path
- Fix an issue of connecting LM Studio api

0.17
- Fix a CLI tool output crash issue
- Fix the issue that the stop button on CLI display window is not shown if the command is on the whitelist
- Limit CLI tool output size
- Better 中文字符支持 in CLI output
- Fix an issue that sometimes the code database service will fail to detect a modified file
- Fix a llm response exception
- Fix an issue that skill is not loaded

0.17.1
- Fix an issue in input window that the cursor will disappear when moving between pasted lines
- Disable some some too aggressive compression
- Add tip for skill

0.18
- Improved context compression quality
- Log the context compression results
- Added an "Evaluate" button for the selected "Compress & Summarize" API on the settings page
- Fixed an issue where showing the code diff would remove breakpoints and bookmarks
- 在代码窗口左侧添加条带来标识当前处于code diff mode下
- In the chat window, collapsed all exploration operations (file reading, keyword searching, etc.)
- Fixed an issue where the chat title generation would occasionally fail
- Recognize symbol link in chat window more smartly(more links recognized)
- 修复了显示skill tip时会crash的问题
- 提高了CLI tool 的命令行兼容性(BASH,CMD)

0.19

- Added MCP support (stdio and URL)
- Added a UI to manage MCPs
- Disabled the bash tool when bash is not available on the current system
- Enabled streaming tool calls for the GLM API
- Fixed an issue where starting a new conversation after rolling back all previous conversations would reset the chat title
- Added cache rate in the cost statistics label
- Fixed an issue where the chat input window could not regain focus when the Visual Studio window was brought back to the foreground
- Restored the cursor to its previous position after the chat input window regains focus
- Fixed an issue where token statistics would become excessively large when using certain endpoints